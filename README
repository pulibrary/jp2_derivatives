64 Bit ONLY!

Dependencies:
 * Imagemagick (convert in particular and exiv2 should be installed. Configure 
   the script so that the constants reference these apps by absolute path 
   (output of `which`). 
 * 64 bit Kakadu is included in lib. Other OSs will need to swap in appropriate 
   libraries.
